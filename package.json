{
  "name": "conductor-firestore-rules",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:pages && npm run test:rules",
    "test:pages": "node --test tests/public-prices-pages.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project conductor-rules-test \"node --test tests/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.28.2"
  }
}
